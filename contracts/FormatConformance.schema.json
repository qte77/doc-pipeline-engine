{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FormatConformance",
  "description": "Output format conformance gate result. Reserved stub — wired in v0.2.",
  "type": "object",
  "required": ["version", "output_format_id", "conformant"],
  "properties": {
    "version": { "const": "0.1.0" },
    "output_format_id": { "type": "string" },
    "conformant": { "type": "boolean" }
  },
  "additionalProperties": false
}
