### Added

- scriv-managed `changelog.d/` per-PR fragments (`changelog_*` make targets) replace the
  hand-edited `## [Unreleased]` section, ending the recurring merge-conflict pain. Adds
  `[tool.scriv]` config, a "Changelog fragments" CONTRIBUTING section, and
  [ADR-0012](docs/adr/0012-scriv-managed-changelog.md). (#129)
